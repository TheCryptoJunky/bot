# Asynchronous functionality for Python
asyncio==3.4.3  # asyncio is part of Python 3.x, this version works for Python 3.8+

# For numerical computations
numpy==1.21.0  # Stable version for most AI-related operations

# To manage environment variables securely
python-dotenv==1.0.0  # Latest stable version

# Garbage collection is part of the Python standard library, no need to specify

# For AI-based LSTM model (assuming you're using a third-party or custom package)
lstm-model==0.2.5  # Assuming this is a custom module, specify a version if you're using a pre-built one

# For ARIMA time-series forecasting (using a common ARIMA library)
statsmodels==0.13.5  # Popular for ARIMA-based time series forecasting

# Reinforcement learning agent package (assuming it's a custom or pre-built package)
stable-baselines3==1.7.0  # Common RL library if you are not using a custom-built RL agent

# Optional: To ensure compatibility with exchanges like Binance
ccxt==1.98.11  # Popular library for working with many exchanges, including Binance, Coinbase, etc.
